import React from "react"
import Layout from "../components/layout"

export default () => (
    <Layout>
        <h1>Cluster's Data</h1>
        <p>
            Here we will have the filters and all the data that integrates a cluster
        </p>
    </Layout>
)